# Qodex 权限与运行边界

本文件描述 Qodex v0.1.0 的 Codex 自主对话模式。旧 DSH 四模式不是本版本的部署入口。

## 会话与管理

- 只接收明确放行的群聊和私聊；空白名单不放行全部，黑名单优先。
- 每个群和私聊使用独立的历史、Codex 会话与长期记忆。人格、聊天口吻、收藏表情及表情备注全局共享。
- 最终回复只能发送到当前会话。模型开始工作后撤销白名单或暂停，后续发送仍会重新检查权限。
- 管理设置通过本机令牌保护的控制台修改；群友聊天不是管理员配置。当前不提供群内 /reset、图形模型选择器或会话重置入口。
- 人格、记忆、网页和 QQ 消息是不同来源的上下文，不能通过文本取得控制台权限。

## Codex 能力边界

桥接为自己的 app-server 进程设置隔离配置，关闭 shell、文件编辑、桌面插件与继承 MCP 工具；使用只读沙箱。模型保留原生联网搜索和图片输入。模型自主对话不注册旧版 QQ 发送/唤醒工具，最终文字和收藏表情由桥接验证后发送。

这些设置依赖兼容的 Codex 运行时。更换版本后应重新验证，不把人格提示当成操作系统隔离。Qodex 不修改用户全局 Codex 设置，不复制官方账户凭据。

## 数据与凭据

安装引导将服务令牌保存在 Windows DPAPI 文件中，启动时通过进程环境传递；Codex 子进程不继承 QBOT_* 或 SNOWLUMA_* 环境变量。Windows 管理日志替换本部署的已知服务令牌。不要主动向模型、群聊或 Issue 提供密码、访问令牌与私人记录。

现有回复路径只替换桥接已知的会话令牌；记忆保存也有有限的凭据格式检查。这些措施不是通用数据防泄漏系统，不保证识别任意秘密。聊天内容及记忆会作为模型输入，经当前 Codex 账户处理；本地保存不意味着离线推理。

## 暂停、退出与备份

暂停时中断进行中的回合，期间收到的消息仍记录但不提交新模型请求，恢复后从新消息继续。停止服务使用启动页或 Control.ps1；关闭网页或启动页不会停止后台。

清空长期记忆只清空这份笔记，不等于删除历史或重置 Codex 会话。备份 config.json、state/ 和自定义记忆目录时按私人资料保管。

更多内容见 [部署指南](docs/DEPLOYMENT.md)、[安全说明](SECURITY.md)。
