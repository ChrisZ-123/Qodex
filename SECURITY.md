# 安全说明

Qodex 把 QQ 消息交给本机 Codex 处理；消息内容、历史、记忆和表情资料可能包含他人信息。仅接入你有权管理的群与私聊，并把控制台、控制接口和 OneBot HTTP/WS 绑定在本机回环地址。项目默认白名单拒绝；安装向导要求明确配置至少一个允许目标。黑名单优先于白名单。

Codex 必须使用当前 Windows 用户的 ChatGPT 登录。OneBot 令牌由设置向导安全输入，控制台与控制接口令牌由本机生成，并保存在 `state/secrets.dpapi`。不要将 `config.json`、`state/`、备份、令牌、带 token 的控制台链接、日志或私人聊天内容提交到仓库或公开 Issue。DPAPI 状态与创建它的 Windows 用户环境相关；迁移时应重新设置所需令牌，而不是改成明文。若怀疑令牌外泄，请在对应服务中轮换并重新初始化本机配置。

发布模式中的 Codex 子进程关闭 shell、文件修改、连接器、插件和多智能体等能力，并采用只读沙箱。发送 QQ 消息由桥接在白名单、暂停与会话检查后执行。原生网页搜索可能把搜索词发送给外部搜索服务；请据此决定哪些会话适合接入。模型可能犯错或输出不当内容，管理员仍需负责账号和群的使用边界。Qodex 不把会话用于自身训练，也不管理 OpenAI 或 SnowLuma 的数据处理政策；使用前阅读各服务的条款和隐私说明。

发现漏洞时，请优先使用仓库 **Security → Report a vulnerability**（若仓库启用私有漏洞报告）；否则通过仓库维护者资料中提供的私有联系方式联络。请勿在公开 Issue、讨论区或 PR 中放入可用凭据、漏洞利用细节和用户数据。报告中说明受影响版本、复现条件、影响范围和一个最小脱敏复现方式。
