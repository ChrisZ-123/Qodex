# 更新记录

## qodex-v0.2.0 — 模型选择与会话重置

- 新增控制台模型和推理强度选择，从账户可用列表中选择，保存后下一轮生效，无须重启，保留速度设置。
- 模型选择对所有会话生效；有任务正在处理时拒绝切换，不自动降级。
- 新增当前会话上下文重置，取消待发送回复，隔离旧任务的迟到结果；保留历史、人格、长期记忆与其他会话。
- 重置后的新模型会话不重新注入此前历史；已发送消息不会撤回。
- 保留 v0.1.0 的部署内记忆目录、首次启动目录创建及控制台登录表单修复。
- 升级沿用现有配置和数据，先停止、备份，再更新依赖、启动并刷新控制台。

## qodex-v0.1.0 — 首个公开预览版

基于 [Derpyu520/qq-bridge 的 dea3ce8 提交](https://github.com/Derpyu520/qq-bridge/commit/dea3ce8) 开发的 Qodex 公开 fork。

- 使用本机 Codex CLI 与已有 ChatGPT 登录处理允许的 QQ 群聊和私聊；启动时核对所选模型及推理强度，不自动切换模型。
- 新增模型自主接话流程、原生网页搜索与图片输入、按会话保存的历史及最多 30,000 字符的长期记忆；同一模型回合同步决定回复与记忆更新。
- 新增本机管理界面：访问权限、全局人格和口吻、暂停、会话历史、长期记忆、收藏表情与备注。
- 群聊 QQ 原生表情可在白名单范围内自动收藏，普通图片和私聊图片不自动收藏；表情目录和看图笔记在允许的会话间共用。
- 新增 Windows 安装向导、启动面板和本机控制脚本；OneBot、控制台与控制接口令牌在本地生成或输入，并使用 Windows DPAPI 保存。

目前只在 Windows 的已有本机环境验证。公开发布后的全新安装、真实 QQ 长时运行和不同 Codex/SnowLuma 版本仍需用户反馈。旧 DSH 内部代码保留，但不作为此版本受支持的部署方式。
